name: get top weekly

on:
  # Automatically runs every 0.00 Sunday
  schedule:
    - cron: '0 0 * * 0'
  
  # Manual trigger for running the workflow
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: checkout repo content
        uses: actions/checkout@v2

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: install system dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y \
          ffmpeg \
          vim \
          && sudo apt-get clean

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Run Python Script
        env:
          REDIT_CLIENT_ID: ${{ secrets.REDIT_CLIENT_ID }}
          REDIT_SECRET_ID: ${{ secrets.REDIT_SECRET_ID }}
          REDIT_USER_AGENT: ${{ secrets.REDIT_USER_AGENT }}
        run: python batch_redit.py
        
